package com.huawei.esdk.ec.device.eserver.multiapponline.convert;

public class AppInfoCapabilityConvert
{
    /*
    public GetAppInfo getAppInfoModal2UDP(String appName, String applyTime)
    {
        GetAppInfo appInfo = new GetAppInfo();
        appInfo.appName = appName;
        
        if (null != applyTime)
        {
            appInfo.applyTime = applyTime;
        }
        
        return appInfo;
    }
    
    public AppInfo getAppInfoUDP2Rest(GetAppInfoAck appInfoAck)
    {
        if (null == appInfoAck)
        {
            return null;
        }
        
        AppInfo appInfo = new AppInfo();
        
        appInfo.setAppDesc(appInfoAck.appDesc);
        appInfo.setAppID(appInfoAck.appId);
        appInfo.setAppLogoUrl(appInfoAck.appLogoUrl);
        appInfo.setAppName(appInfoAck.appName);
        appInfo.setResult(String.valueOf(appInfoAck.result));
        
        return appInfo;
    }
    
    */}
